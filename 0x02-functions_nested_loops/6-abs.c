#include "main.h"

/**
 * _abs - function to calculate the absolute value of an integer
 *
 * @n: takes in integer type inpute for function
 *
 * Return: Always 0 (sucess)
 */

int _abs(int n)
{
	if (n, 0)
		n = (-1) * n;
	return (n);
}
